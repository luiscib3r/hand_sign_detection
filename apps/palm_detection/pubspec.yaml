name: palm_detection
description: Palm Detection Demo App

publish_to: none

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  camera: ^0.10.5+5
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.1
  image: ^4.1.3
  tflite_flutter: ^0.10.3

dev_dependencies:
  build_runner: ^2.4.7
  freezed: ^2.4.5
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true

  assets:
    - assets/palm_detection_full.tflite
    - assets/anchors.csv
